#include "main.h"
/**
 * print_alphabet - print the letters of the alphabet
 * Retur: return 0 for the requirements
 */
void print_alphabet(void)
{
char letter;
for (letter = 'a'; letter <= 'z'; letter++)
	_putchar(letter);
_putchar('\n');
}
